# Хувь нэмэр оруулах / Оролцох

## Банкны жагсаалт шинэчлэх (хамгийн түгээмэл)

`src/lib/banks.js` файлын `BANKS` массив нь албан ёсны домэйнуудыг агуулна.
Зөвхөн **баталгаажсан, албан ёсны** домэйн нэмнэ үү (эх сурвалж: Монголбанк
болон банк тус бүрийн албан ёсны сайт).

```js
{ name: "Банкны нэр", domains: ["undsen.mn", "digital.undsen.mn"] },
```

Кирилл нэрээр бичсэн хуурамчийг илрүүлэхийн тулд `CYRILLIC` объектод банкны
монгол нэрийг нэмнэ:

```js
"ханбанк": "Хаан Банк",
```

## Тест

Кодын логикийг шалгах:
- Node: `node -e "require('./src/lib/banks.js');require('./src/lib/idn.js'); console.log(TATAR_IDN.analyzeHost('хacbank.mn'))"`
- Браузер: `test/logic-test.html`-ийг нээхэд бүх кейс PASS байх ёстой.

## Дүрэм
- Гуравдагч этгээдийн сервер рүү өгөгдөл илгээхгүй (offline-first).
- Шинэ зөвшөөрөл (permission) нэмэхээс зайлсхийх.
